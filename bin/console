#!/usr/bin/env bash
# IRB with the app booted. Sources .env if present.
set -euo pipefail
cd "$(dirname "$0")/.."

bin/bootstrap
# Dev only: production gets its env from the platform and has no .env.
if [ -f .env ]; then
  set -a
  . ./.env
  set +a
fi

exec bundle exec irb -r ./lib/grey/boot
